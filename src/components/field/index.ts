import styles from './field.module.scss';

const Field =
    `
        <div class="${styles.field}">
            <div>{{name}}</div>
            <div>{{value}}</div>
        </div>
    `

export {Field};
